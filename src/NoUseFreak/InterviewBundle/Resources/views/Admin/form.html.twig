{% extends '::base.html.twig' %}

{% block body %}
    <div>
        <h1>Admin - Users</h1>

        <div class="clearfix">
            <a href="{{ path(routes.list) }}" class="btn btn-primary pull-right">List</a>
        </div>

        <form action="" method="post">
            {{ form_widget(form) }}

            <button type="submit">Save</button>
        </form>

    </div>
{% endblock %}
